const config = {
    backendURL: 'http://localhost:3000/api', // Centralized backend URL
    // backendURL: 'https://clockrepo-backend.ce-data.net/api', // Centralized backend URL
};

export default config;